Never hardcode colours.

Never duplicate business logic.

Always memoise expensive components.

Prefer composition.

Never exceed 300 lines per component.

Prefer FlatList.

Use React Native Paper.

Use Theme Tokens.

No inline styles.

No duplicated utilities.